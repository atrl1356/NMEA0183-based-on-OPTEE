修改main.c

開啟optee時，生成一張虛擬網卡
make run-only QEMU_EXTRA_ARGS="-netdev user,id=vmnic,hostfwd=udp::10110-:10110 -device virtio-net-pci,netdev=vmnic"
進CA輸入ifconfig -a確認虛擬網卡執行(lo是默認，應該會有eth0)
接著下行啟動網卡
ifconfig eth0 10.0.2.15 netmask 255.255.255.0 up
理論上，CA可以收到0.0.0.0的封包
